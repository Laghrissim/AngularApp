import { PasswordResetDTO } from './password-reset-dto.model';

describe('PasswordResetDTO', () => {
  it('should create an instance', () => {
    expect(new PasswordResetDTO()).toBeTruthy();
  });
});
